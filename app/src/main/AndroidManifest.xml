<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.dux.p013ab">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.LightNoActionBar">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Dashboard"
            android:label="@string/title_activity_dashboard" />
        <activity android:name=".more_websites.Websites"
            android:label="@string/websites"/>
        <activity android:name=".placement.Placements"
            android:label="@string/placement"/>
        <activity android:name=".notices.Notices"
            android:label="@string/notices"/>
        <activity
            android:name=".news_and_events.NewsAndEvent"
            android:label="@string/news_and_event" />
    </application>

</manifest>